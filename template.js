const a = 10;
const b = 20;
const number = [10, 20, 30, 40, 50];
const summary = 'sub of:' + a + 'and' + b + 'is' + (a + b);
console.log(summary);
const newSummary = `
sum of ${a} and ${b} is : ${a + b};
`
console.log(newSummary);